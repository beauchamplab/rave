# Function to check if data repository has data

Function to check if data repository has data

## Usage

``` r
any_subject_loaded(rave_data = getDefaultDataRepository())
```

## Arguments

- rave_data:

  internally used
