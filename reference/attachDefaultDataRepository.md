# Attach subject data

Attach subject data

## Usage

``` r
attachDefaultDataRepository(
  unload = TRUE,
  rave_data = getDefaultDataRepository()
)
```

## Arguments

- unload:

  TRUE if you want to detach

- rave_data:

  internally used
