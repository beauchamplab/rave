

# Exports
*(Automatically Generated by RAVE)*

## Parameter Settings

%s
* Changing Variable: `%s`

## Outputs

%s

## Results:

```{r, echo = FALSE, message = FALSE, warning = F, error = F}
rave_options(logger_enabled = FALSE)
if(!exists('par.settings')){
  par.settings = par()
}

module = make_module('%s', '%s')
iter_results(
  module, inputId = '%s',
  valueList = %s, outputs = %s,
  param = %s, execute = F, par.settings = par.settings
) ->
  results
```
